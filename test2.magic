known tome TestTome {
  secret silent GrandRitual(rune[] args) {
    if (args.Length = 0) {
      conjure Curse("You need to input a number/rune!")
    }

    law ascii = cast IntToAscii(42)
    cast Scribe(ascii)
  }
}